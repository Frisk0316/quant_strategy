# OKX API credentials — copy to .env and fill in real values
# IMPORTANT: Never commit .env to version control
# API key should NOT have Withdraw permission; bind to static IP

OKX_API_KEY=your_api_key_here
OKX_SECRET=your_secret_here
OKX_PASSPHRASE=your_passphrase_here

# Deribit testnet only: trade-scoped, no withdrawal, and separate from any future live key.
DERIBIT_API_KEY=
DERIBIT_API_SECRET=

# Binance Spot Test Network only: trade/read permissions, no withdrawal.
BINANCE_API_KEY=
BINANCE_SECRET=

# Binance USD-M demo/testnet only: separately issued key, no withdrawal.
BINANCE_FUTURES_API_KEY=
BINANCE_FUTURES_SECRET=

# Telegram bot for alerts & remote kill switch
TELEGRAM_TOKEN=your_bot_token_here
TELEGRAM_CHAT_ID=your_chat_id_here

# Market-data ingestion / TimescaleDB
TIMESCALE_PASSWORD=changeme
DATABASE_URL=postgresql://quant:changeme@localhost:5432/quant
REQUEST_TIMEOUT_SECONDS=20
HTTP_MAX_RETRIES=5
HTTP_BACKOFF_SECONDS=1.5
FLUSH_EVERY_REQUESTS=10
FLUSH_MAX_ROWS=50000
FLUSH_EVERY_SECONDS=60
FRED_API_KEY=

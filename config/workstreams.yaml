# Hand-maintained source for the Progress panel.
# Update this alongside docs/AI_HANDOFF.md at session end.
# status: active | blocked | done | shelved
# Milestones before `current` are done; `current` is in progress; the rest pending.
workstreams:
  - name: XS momentum
    status: blocked
    milestones: [spec, impl, backtest, wf_cpcv, demo, live]
    current: wf_cpcv
    state: "PSR 0.82 < 0.95; promotion BLOCKED; xs_momentum stays disabled"
    next: "re-run after portfolio-vol sizing decision; raise PSR >= 0.95"
    links: [docs/superpowers/plans/2026-06-23-xs-momentum-universe.md]
    updated: 2026-06-25

  - name: Strategy research pipeline (batch 2 C1/C2/C3)
    status: shelved
    milestones: [spec, impl, backtest, wf_cpcv, review]
    current: review
    state: "batch 2: C1 refuted; C2 realism re-cost refuted H-007 (DSR 0.004, PSR 0.446); C3 Stage-3 refuted H-008 (DSR 0.453, PSR 0.581) after 2026-06-30 F&G published_at as-of hardening"
    next: "No adapter/publish/demo/shadow/live for C1/C2/C3 without a new Claude/user-approved research path"
    links: [docs/superpowers/plans/2026-06-25-strategy-research-pipeline-stage1.md, docs/superpowers/plans/2026-06-29-strategy-research-pipeline-stage2.md, tasks/2026-06-29-c2-funding-carry-realism-task.md]
    updated: 2026-06-30

  - name: Strategy research pipeline — full-auto roadmap
    status: active
    milestones: [diagnose, draft_specs, user_review, checkpoint1_impl, ingestion_impl, orchestrator_spec_hardened, orchestrator_impl, first_batch_review, improvement_p1_p8, real_data_runs]
    current: real_data_runs
    state: "Real-data acceptance DONE and all three 2026-07-03 user decisions executed: P1 funding 66,041 rows/22 symbols/0 gaps; P8 Vision OI 262,814 rows each BTC/ETH (F-OI-POSITIONING unblocked); P5 liquidation ingest scheduled every 2h (quant_liq_okx_ingest schtasks, Interactive-only, RUNBOOK); Stage-2 breadth warmup window changed with user approval (breadth_warmup_days=30, manifest 2026-07-03-stage2-breadth-warmup.md, threshold values unchanged). Post-change probes: official membership still honestly FAILs (good 7/10, blocked on membership artifact); DB-universe diagnostic reads data_availability=PASS (good 28/10, min 24/10)."
    next: "Codex: P9 membership-builder DB source + shared parquet rebuild (task file; window already fixed, do not re-touch). After P9: rerun official funding probe; if PASS, Claude writes the F-FUNDING-XS-DISPERSION Stage-1 spec. Do not schedule the orchestrator or claim promotion/live readiness."
    links: [tasks/2026-07-03-pipeline-improvement-tasks.md, docs/superpowers/specs/2026-07-03-statistical-power-gates.md, docs/superpowers/specs/2026-06-30-stage3-idea-ingestion-design.md, docs/superpowers/specs/2026-06-30-mechanism-taxonomy.md, docs/superpowers/specs/2026-07-01-pipeline-orchestration-driver-design.md]
    updated: 2026-07-03

  - name: Multi-venue instrument specs (ADR-0007 P1)
    status: done
    milestones: [spec, impl, validation, consolidated_pr, merge]
    current: merge
    state: "Already merged into main (origin branch is 0 ahead / 19 behind origin/main); branch still exists"
    next: "none - ADR-0007 P1 complete; no cleanup action taken"
    links: [docs/superpowers/plans/2026-06-17-multi-venue-instrument-specs-p1.md]
    updated: 2026-06-29

  - name: Repo maintenance (M1-M5, non-pipeline)
    status: done
    milestones: [audit, m1_ci_consistency, m2_doc_slimming, m3_smoke_fixture, m4_monitoring_tests, m5_stocks_decision, claude_review, m2_r1_history_migration]
    current: m2_r1_history_migration
    state: "M1-M5 ACCEPTED. M2-R1 Claude-reviewed 2026-07-03 and ACCEPTED on independent checks: all three verbatim spot-checks hit (batch-1 'do not tune to chase the gate', C2 DSR 0.0041 / WF -1.5093 / n_trials=48, DSR<=PSR(0) + both untrusted artifact names), 8/8 source-unique strings present in CHANGELOG_AI, both KNOWN_ISSUES caveats restored, AI_HANDOFF 205 lines <= 400, no stale dirty-worktree claims, docs checks pass."
    next: "Commit the M2-R1 docs remediation (uncommitted working-tree docs) when the user asks; run make verify/full only in an environment with make, TimescaleDB, and required data."
    links: [tasks/2026-07-03-project-maintenance-tasks.md]
    updated: 2026-07-03

  - name: User manual
    status: done
    milestones: [draft, write, verify]
    current: verify
    state: "All chapters incl. 90-research-pipeline written, verified, and committed (fac6386)"
    next: "none - manual complete; rerun scripts/generate_research_pipeline_doc.py if content changes"
    links: [docs/superpowers/plans/2026-06-25-user-manual.md]
    updated: 2026-06-29

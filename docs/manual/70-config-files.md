# 設定檔總覽

本章待補。目前請先參考 source docs（這些就是各自的真值來源）：

- 系統模式 / 標的 / 權益：`config/settings.yaml`
- 各策略參數：`config/strategies.yaml`（見「策略與其參數」章）
- 風控限制與回測執行參數：`config/risk.yaml`（見「風控限制」章）

> config 是 single source of truth；本手冊只解釋意義與理由，數值以 config 為準。

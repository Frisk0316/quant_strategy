---
status: current
type: review
owner: claude
created: 2026-07-18
last_reviewed: 2026-07-18
expires: none
superseded_by: null
---

# Claude Review: E-057 (H-010 Stage-2 shelve)

Reviewed per docs/REVIEW_QUESTIONS.md + docs/CRITIQUE_PROTOCOL.md. Two scout
agents (code facts; docs/artifacts) plus direct recomputation of the power
floor by the reviewing session. Codex handoff:
`tasks/2026-07-18-h010-e057-session-handoff.md`.

## Verdict

**APPROVE.** The Stage-2 shelve of H-010 is correct, honestly accounted, and
fail-closed. No blocker or major finding. Do not rerun or retune H-010.

## Verification evidence

- **Power floor 0.8838 reproduced exactly**: reviewer ran
  `min_detectable_sharpe(breadth=1.5, n_obs=2268, n_trials=4)` →
  0.8838161257569653 (matches `h010_power_input.json`). Also confirmed the
  pre-reg spec's 0.8682 corresponds to n_obs≈2350, validating the task file's
  diagnosis; the correction to 2,268 formal days (calibration excluded) is
  stricter, i.e. conservative. Immaterial to verdict since frozen
  plausible_net_sharpe=0 fails any positive floor.
- **Cost math verified in code**: gross = position*(okx_open/entry_price−1)
  ×10,000 bps; round-trip = maker 2+2 entry + maker 2+2 mean-reversion exit
  (8.0 bps) or taker 5+2 max-hold exit (11.0 bps); gate is
  median(gross) > median(cost) per pre-registration. 1.3636 bps ≪ 8.0 bps over
  7,376 episodes; verdict robust (~5.9× shortfall even at best-case cost).
- **No lookahead**: signal at bar t close → fill at bar t+1 okx_open;
  z-window backward-only; venue closes synchronous (I19/I47 respected).
- **Funding fail-closed (R3.4/F47/I48)**: query is `WHERE source='okx'` only;
  0 rows → funding.complete=false → data_availability FAIL; no Binance
  substitution path exists; guarded by `tests/unit/test_xvenue_leadlag_probe.py`
  (funding sign also verified: positive rate → longs pay).
- **Zero-trial accounting is honest (R6.3)**: single anchor (grid combo 3,
  L240/Z2) frozen ex-ante, evaluated only on [2020-01-01, 2020-04-01), which
  is excluded from the 2,268-day formal window; no grid loop exists in code;
  `grid_trials_evaluated=0`; prospective n_trials=4 used in the power screen
  (conservative). Stop-on-fail cannot inflate selection bias. Ledger
  cumulative stays 0, K stays 0/2 — consistent across artifact/registry/ledger.
- **I45 sequencing**: registry validates schema/kind/payload SHA-256/anchor
  match of the frozen calibration evidence before opening the active DB
  connection; missing/mismatched evidence raises ValueError (fail-closed).
- **Artifacts**: hashes in EXPERIMENT_REGISTRY match the on-disk
  `results/h010_e057_stage2_20260718/` payloads; artifact write is
  FileExistsError-protected (immutable).
- **No readiness claim**: idealized_fill=true disclosed; provisional net
  Sharpe −12.7264 explicitly not accepted as measured; shelve is Stage-2
  data/economics, not a Stage-3 refutation — R7.1/R7.2 respected.

## Findings (all minor/nit; none block the shelve)

1. **Minor — R3.4 added without ADR.** New numbered rule + I48 + F47 shipped
   via Change Manifest only. Acceptable as a fail-closed clarification within
   the existing R3/R6.4 provenance principle (handoff states ADR N/A,
   ADR-0013 authoritative); if R3.4 is later generalized beyond H-010-style
   cross-venue strategies, add an ADR then.
2. **Minor — distinctness is structurally unpassable in this design**: the
   ≥365-common-day requirement cannot be met on a ~91-day calibration sample,
   so the check can only fail closed here. Harmless for a shelve; any future
   pipeline that expects distinctness to PASS at Stage 2 must measure it on
   the formal window, not the calibration slice.
3. **Nit — cost gate compares medians**, which could in principle mask a
   tail-driven edge; it follows pre-registration verbatim and the −12.7
   provisional Sharpe rules out a tail rescue here. No action.
4. **Constraint to preserve**: if H-010 is ever revived under a genuinely new
   mechanism, [2020-01-01, 2020-04-01) has been observed and must stay
   excluded from any formal validation window.

## Next action

Per handoff and CURRENT_STATE item 6: next ideation round only with a
genuinely new data family/mechanism; filling the OKX funding gap cannot
repair the failed gross-vs-cost inequality.

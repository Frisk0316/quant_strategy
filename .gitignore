.env
__pycache__/
*.py[cod]
*.egg-info/
dist/
build/
.venv/
venv/
*.log
data/ticks/
data/universe/
data/parquet/
data/literature_cache/
*.db
*.sqlite
.DS_Store
.pytest_cache/
.ruff_cache/
notebooks/.ipynb_checkpoints/

# Claude Code local settings (personal allowlist, not shared)
.claude/settings.local.json

# Backtest run artifacts (timestamped dirs with CSV/JSON per run)
results/*/

# Calibration JSONL session logs
results/calibration/

# Runtime logs (*.log already covered, add compressed variants)
logs/
*.log.gz

# Local research workspaces and scratch are not shared via GitHub, EXCEPT the
# crypto-alpha-lab (the shared literature ingestion / idea-generator A-half).
research/*
!research/crypto-alpha-lab/
